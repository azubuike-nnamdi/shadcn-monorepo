import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title="Style Guide/Colors" />

# Colors
#### Application of the color palette brings a unified and recognizable consistency to MTN’s array of digital products and interfaces. This consistency is grounded in a set of well-defined rules about how to work with the Sunshine component library in the context of dark and light themes.
### Color Anatomy
#### The neutral gray family is dominant in the default themes, making use of subtle shifts in value to organize content into distinct zones. The core yellow family serves as the primary action color across all MTN products and experiences. Additional colors are used sparingly and purposefully.

<div class="pt-32">
  <ColorPalette>
    <ColorItem
      title="Primary"
      subtitle="This is primary color"
      colors={{
        "#FFCD05": "hsl(var(--primary))"
      }}
    />

    <ColorItem
      title="Secondary"
      subtitle="This is secondary color"
      colors={{
        "#000000": "hsl(var(--secondary))"
      }}
    />

    <ColorItem
      title="Foreground"
      subtitle="This is foreground color"
      colors={{
        "foreground": "hsl(var(--primary))",
      }}
    />

    <ColorItem
      title="Card"
      subtitle="This is card color"
      colors={{
        "card": "hsl(var(--card))",
        "card-foreground": "hsl(var(--card-foreground))",
      }}
    />

    <ColorItem
      title="Popover"
      subtitle="This is popover color"
      colors={{
        "popover": "hsl(var(--popover))",
        "popover-foreground": "hsl(var(--popover-foreground))",
      }}
    />

    <ColorItem
      title="Muted"
      subtitle="This is muted color"
      colors={{
        "muted": "hsl(var(--muted))",
        "muted-foreground": "hsl(var(--muted-foreground))",
      }}
    />

    <ColorItem
      title="Accent"
      subtitle="This is accent color"
      colors={{
        "accent": "hsl(var(--accent))",
        "accent-foreground": "hsl(var(--accent-foreground))",
      }}
    />
  
    <ColorItem
      title="Destructive"
      subtitle="This is destructive color"
      colors={{
        "destructive": "hsl(var(--destructive))",
        "destructive-foreground": "hsl(var(--destructive-foreground))",
      }}
    />

    <ColorItem
      title="Momo"
      subtitle="This is momo color"
      colors={{
        "momo": "hsl(var(--momo))",
        "momo-foreground": "hsl(var(--momo-foreground))",
      }}
    />

    <ColorItem
      title="Border"
      subtitle="This is border color"
      colors={{
        "border": "hsl(var(--border))",
        "border-foreground": "hsl(var(--border-foreground))",
      }}
    />

    <ColorItem
      title="Input"
      subtitle="This is input color"
      colors={{
        "input": "hsl(var(--input))"
      }}
    />

    <ColorItem
      title="Ring"
      subtitle="This is ring color"
      colors={{
        "ring": "hsl(var(--ring))"
      }}
    />

    <ColorItem
      title="Radius"
      subtitle="This is radius color"
      colors={{
        "radius": "hsl(var(--radius))"
      }}
    />

     <ColorItem
      title="Chart"
      subtitle="This is chart colors"
      colors={{
        "chart-1": "hsl(var(--chart-1))",
        "chart-2": "hsl(var(--chart-2))",
        "chart-3": "hsl(var(--chart-3))",
        "chart-4": "hsl(var(--chart-4))",
        "chart-5": "hsl(var(--chart-5))",
      }}
    />
  </ColorPalette>
</div>